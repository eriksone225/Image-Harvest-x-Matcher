# Image/Video Harvester x Matcher GUI v14

## Fix in v14

The Start Harvest button is visible again.

The Harvest tab now uses:
- Left side: scrollable settings area
- Right side: pinned Run panel

So you can scroll through all settings without losing:
- Start Harvest
- Stop
- Open Harvest Output
